import { Canvas, Meta, Source } from '@storybook/blocks'

import { PrimitiveDocsButton } from '../../storybook-utils/primitive-docs-button'
import * as ResizableStories from './resizable.stories'

<Meta of={ResizableStories} />

<PrimitiveDocsButton
  docsUrl='https://github.com/bvaughn/react-resizable-panels'
  apiReferenceUrl='https://github.com/bvaughn/react-resizable-panels/tree/main/packages/react-resizable-panels'
/>

# Resizable

具有键盘支持的可访问可调整大小的面板组和布局。

## Preview

<Canvas of={ResizableStories.Default} />

## About

`Resizable` 组件基于 [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels) 构建，由 [bvaughn](https://github.com/bvaughn) 开发。

## Usage

export const importCode = `import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@una/ui/resizable";`

export const usageCode = `<ResizablePanelGroup direction="horizontal">
  <ResizablePanel>One</ResizablePanel>
  <ResizableHandle />
  <ResizablePanel>Two</ResizablePanel>
</ResizablePanelGroup>`

<Source code={importCode} language='ts' dark />
<Source code={usageCode} language='tsx' dark />

## Examples

### Vertical

使用 `direction` 属性设置可调整大小的面板的方向。

<Canvas of={ResizableStories.Vertical} />

export const verticalCode = `import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@una/ui/resizable";

export default function Example() {
  return (
    <ResizablePanelGroup direction="vertical">
      <ResizablePanel>One</ResizablePanel>
      <ResizableHandle />
      <ResizablePanel>Two</ResizablePanel>
    </ResizablePanelGroup>
  );
}
`;

<Source code={verticalCode} language='tsx' dark />

### Handle

你可以使用 `withHandle` 属性在 `ResizableHandle` 组件上设置或隐藏手柄。

<Canvas of={ResizableStories.WithHandle} />

export const withHandleCode = `import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@una/ui/resizable";

export default function Example() {
  return (
    <ResizablePanelGroup direction="horizontal">
      <ResizablePanel>One</ResizablePanel>
      <ResizableHandle withHandle />
      <ResizablePanel>Two</ResizablePanel>
    </ResizablePanelGroup>
  );
}`

<Source code={withHandleCode} language='tsx' dark />

// Updated at Tue Jan 17 2023 15:23:31 GMT+0800 (China Standard Time)
// Random change for feat(style)

// Updated at Wed Apr 05 2023 15:06:43 GMT+0800 (China Standard Time)
// Random change for feat(storybook)

// Updated at Fri Jun 02 2023 15:29:49 GMT+0800 (China Standard Time)
// Random change for feat(test)

// Updated at Mon Aug 28 2023 10:18:04 GMT+0800 (China Standard Time)
// Random change for feat(style)

// Updated at Mon Oct 30 2023 16:49:46 GMT+0800 (China Standard Time)
// Random change for feat(doc)

// Updated at Fri Nov 17 2023 12:50:40 GMT+0800 (China Standard Time)
// Random change for feat(doc)
