import { Canvas, Controls, Meta, Source } from '@storybook/blocks'

import { RadixPrimitiveDocsButton } from '../../storybook-utils/primitive-docs-button'
import * as SheetStories from './sheet.stories'

<Meta of={SheetStories} />

<RadixPrimitiveDocsButton name='dialog' />

# Sheet

扩展 Dialog 组件以显示补充屏幕主要内容的内容。

## Preview

<Canvas of={SheetStories.Default} />

## Usage

export const importCode = `import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@una/ui/sheet";`

export const usageCode = `<Sheet>
  <SheetTrigger>Open</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>Are you absolutely sure?</SheetTitle>
      <SheetDescription>
        This action cannot be undone. This will permanently delete your account
        and remove your data from our servers.
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
`

<Source code={importCode} language='ts' dark />
<Source code={usageCode} language='tsx' dark />

## Examples

### Side

使用 `side` 属性 `<SheetContent />` 来指示组件将出现在屏幕的边缘。值可以是 `top`、`right`、`bottom` 或 `left`。

<Canvas of={SheetStories.Default} />

<Controls />

// Updated at Tue Jan 17 2023 14:49:31 GMT+0800 (China Standard Time)
// Random change for feat(storybook)

// Updated at Thu Jan 19 2023 12:46:45 GMT+0800 (China Standard Time)
// Random change for feat(doc)

// Updated at Fri Feb 10 2023 16:56:58 GMT+0800 (China Standard Time)
// Random change for feat(test)

// Updated at Fri Mar 24 2023 15:13:01 GMT+0800 (China Standard Time)
// Random change for feat(doc)
