import * as React from 'react';

// * --------------------------------------------------------------------------- comp

export const Button = () => {
  return (
    <div>
      <button>this is react component --</button>
    </div>
  );
};
